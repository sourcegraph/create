env:
  FORCE_COLOR: '1'

steps:
  - command: |-
      npm ci
      npm run prettier
      npm run tslint
      npm run build
    label: ':typescript:'

  - wait

  - command: |-
      npm ci
      npm run build
      npm run semantic-release
    branches: master
    label: ':npm:'
